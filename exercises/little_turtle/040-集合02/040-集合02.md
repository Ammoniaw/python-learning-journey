# 040 集合 02 

## 问答题

### 0.请问下面代码会打印 True 还是 False 呢

- 代码

  ```
  >>> s1 = set([1, 1, 2, 3, 5])
  >>> s2 = frozenset([1, 1, 2, 3, 5])
  >>> s1 == s2
  ```

- True
- 小甲鱼补充：虽然一个是可变集合，一个是不可变（冻结）集合，但它们包含的元素是一样的。但注意，如果是拿相同元素的列表和元组进行比较，则结果会是 False

### 1.既然有了 [set()](https://fishc.com.cn/thread-203522-1-1.html) 集合，为什么还要创造 [frozenset()](https://fishc.com.cn/thread-203568-1-1.html) 来冻结集合（换句话说，[frozenset()](https://fishc.com.cn/thread-203568-1-1.html) 的优势是什么）？

- 冻结的集合不能添加或者删除元素，就和元组一样。

- 小甲鱼补充：只有使用 frozenset 对象，才可以创建嵌套集合（因为集合内部的元素必须是可哈希的）。

- 代码示例

  ```python
  # 使用 set 对象，无法创建嵌套集合
  >>> x = {1, 2, 3}
  >>> y = {x, 4, 5}
  Traceback (most recent call last):
    File "<pyshell#39>", line 1, in <module>
      y = {x, 4, 5}
  TypeError: unhashable type: 'set'
  
  # 改用 frozenset 对象，则解决问题
  >>> x = frozenset(x)
  >>> y = {x, 4, 5}
  >>> y
  {frozenset({1, 2, 3}), 4, 5}
  ```

  

### 2.集合中，difference(*others) 和 difference_update(*others) 这两个方法都是用于计算差集，那么它们有什么区别呢？

- 顾名思义，计算差集与计算差集并更新集合
- 小甲鱼详细说明：[difference(*others)](https://fishc.com.cn/thread-203569-1-1.html) 方法是计算差集并将结果返回为一个新的集合，[difference_update(*others)](https://fishc.com.cn/thread-203569-1-1.html) 方法则是直接使用新的结果更新集合。因此，前者 set 和 frozenset 对象均使用，后者仅 set 对象可用。

### 3. 由于 frozenset 对象是不可修改的，所以我们说它也是可哈希的，对吗？

- 对。

- 小甲鱼代码说明

  ```python
  >>> s = frozenset("1, 1, 3, 4, 5")  # 冻结列表可以实现嵌套集合
  >>> hash(s)
  5703935143154763776
  ```

### 4. 请问集合对象的 update(*others) 方法和 add(item) 方法有什么不同？

- [update(*others)](https://fishc.com.cn/thread-203569-1-1.html) 方法使用 others 容器中的元素来更新集合；[add(elem)](https://fishc.com.cn/thread-203569-1-1.html) 方法则是单纯地往集合里面添加一项数据。

- 小甲鱼详细说明

  ```python
  假设将字符串 "FishC" 作为参数，使用 update() 方法它是拆分成 5 个字符分别添加进去，而使用 add() 方法则是将整个字符串作为一个元素添加进去。
  
  >>> s = set([1, 1, 2, 3, 5])
  >>> s.update("FishC")
  >>> s
  {1, 2, 3, 'F', 5, 'i', 's', 'C', 'h'}
  >>> s = set([1, 1, 2, 3, 5])
  >>> s.add("FishC")
  >>> s
  {1, 2, 3, 5, 'FishC'}
  ```

  

### 5. 请问集合对象的 `remove(elem)` 方法和 `discard(elem)` 方法有什么不同？

- 如果指定的元素不存在，[remove()](https://fishc.com.cn/thread-203569-1-1.html) 方法将会抛出异常，[discard()](https://fishc.com.cn/thread-203569-1-1.html) 方法则不会。

### 6. 请问下面代码输出的内容是什么？

- 代码

  ```
  >>> {x for x in 'FishCChsiF' if x not in 'ish'}
  ```

- {'F', 'C'}

---

## 动动手

### 0. 最开始的时候，Python 是没有 [set()](https://fishc.com.cn/thread-203522-1-1.html) 的，那么当时伟大的程序猿是如何实现 “集合” 这个的概念呢？

- 说明

  ```python
  现在我们将时间回拨到那个 “一穷二白” 年代，现在大家没有 set() 可用了，只能利用 dict() 来实现交集和并集。
  题目要求：
  
  生成一个随机数列表，一共有 100 个元素，每个元素取 1~100 的随机值，赋值给变量 x
  生成另一个随机数列表，一共有 100 个元素，每个元素取 50~150 的随机值，赋值给变量 y
  利用字典的 “键” 不会重复的特点，计算 x 和 y 的交集（就是两者共有的元素)
  ```

- 小甲鱼

  ```python
  >>> import random
  >>> x = [random.randint(1, 100) for i in range(100)]
  >>> y = [random.randint(50, 150) for i in range(100)]
  >>> inter = dict.fromkeys([z for z in x if z in y])
  >>> list(inter.keys())
  [63, 58, 57, 50, 56, 53, 91, 100, 98, 51, 61, 73, 79, 82, 75, 86, 77, 74, 99, 93, 89]
  ```

### 1.破解 MD5 哈希加密！！

- 说明:在前面的课后作业中（[传送门](https://fishc.com.cn/thread-203083-1-1.html)），我们提到过通常服务器是不会保存用户的明文密码的（那样就太不安全了），取而代之的是存储单向哈希加密后的密文。
  不过尽管如此，也并非绝对的安全！
  虽然说单向哈希加密是无法逆向，但聪明的黑客们想出了彩虹表破解法：

  > 彩虹表是一个用于加密散列函数逆运算的预先计算好的表，常用于破解加密过的密码散列。彩虹表常常用于破解长度固定且包含的字符范围固定的密码（如信用卡、数字等）。这是以空间换时间的典型实践，比暴力破解（Brute-force attack）使用的时间更少，空间更多；但与储存密码空间中的每一个密码及其对应的哈希值（Hash）实现的查找表相比，其花费的时间更多，空间更少。

- 密码及其对应的哈希值（Hash）实现的查找表相比，其花费的时间更多，空间更少。
  说人话就是：所谓的彩虹表，其实就是将所有可能的字符串组合都给预先哈希一遍，然后将映射结果保存起来，当黑客拿到一个哈希值时，通过查表的方式获取密码明文。虽然密码安全宣传多年来都有在开展，但还是难以阻止使用出生日期等 6 位数字的人们，这一次，我们就用实际行动告诉大家，6 位数字的哈希密码有多容易被破解……

- 要求

  ```python
  生成 0~999999 所有整数组成密码的哈希值
  将上面生成的哈希值保存为映射类型
  通过查表的方式，计算下面 3 个哈希值对应的明文密码
  ```

- 明文密码

  ```python
  021bbc7ee20b71134d53e20206bd6feb
  e10adc3949ba59abbe56e057f20f883e
  655d03ed12927aada3d5bd1f90f06eb7
  ```

- 提示

  ```python
  hashlib.md5() 的参数是需要一个 b 字符串（即 bytes 类型的对象），这里可以使用 bytes("123", "utf-8") 的方式将 "123" 转换为 b"123"。
  ```

- 小甲鱼

  ```python
  import hashlib
      
  rainbow_table = {}
      
  for i in range(1000000):
      bstr = bytes(str(i), "utf-8")
      s = hashlib.md5(bstr).hexdigest()
      rainbow_table[s] = i
      
  s1 = "021bbc7ee20b71134d53e20206bd6feb"
  s2 = "e10adc3949ba59abbe56e057f20f883e"
  s3 = "655d03ed12927aada3d5bd1f90f06eb7"
      
  print(f"{s1} 对应的明文是：{rainbow_table[s1]}")
  print(f"{s2} 对应的明文是：{rainbow_table[s2]}")
  print(f"{s3} 对应的明文是：{rainbow_table[s3]}")
  ```

  



