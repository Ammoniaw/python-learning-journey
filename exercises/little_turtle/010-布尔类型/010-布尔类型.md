# 010-布尔类型

## 问答题

### 0. 请问 Python 是否支持链式比较？

- 支持。

- 示例

  ```python
  3 < 4 < 5;链式比较 ==>相当于 3 < 4 and 4 < 5
  ```

### 1. 请问下面两段代码有什么区别呢？

- 代码1

  ```
  >>> if bool(250):
  ...     print("Yeah, you are right.")
  ```

- 代码2

  ```
  >>> if 250:
  ...     print("Yeah, you are right.")
  ```

- 运行结果是一样的，if语句的条件判断都为真

### 2. 在 Python 中，所有的对象都可以进行真值检测，对吗？

- 正确

### 3. 请问下面表达式的值是什么？

- 代码

  ```
  >>> not 3 == 5
  ```

- ~~False~~；True

  ```python
  执行的优先级顺序不同，首先判断 3 == 5；随后执行 not 进行取反操作。
  ```

### 4. 在 Python 中，True 和 False 两个关键字是完全等值于 1 和 0 的，对吗？

- ~~应该不是，如果完全等值，是不是说明这两个可以参与数值运算了。~~
- 正确，布尔类型就是特殊的整数类型；但作为if语句或者while循环等条件判断，使用True和False更加清晰

### 5. 请问下面代码打印的内容是什么？

- 代码

  ```
  >>> print(5 > 3 and 4)
  ```

- ~~1~~；4

  ```python
  执行过程
  5 > 3 返回 True,1 and 4, and取最后的值，所以为4
  ```

### 6. 请问下面代码打印的内容是什么？

- 代码

  ```python
  >>> from fractions import Fraction
  >>> print(Fraction(1, 2) * 2)  # 分数 1/2 * 2
  ```

- 1

---

## 动动手

### 0. 请自学 [fractions](https://fishc.com.cn/thread-148763-1-1.html) 文档，并计算 1708227363155544/4636617128565048 约分后的值。

- make_fraction.py

  ```python
  # -*- coding: utf-8 -*-
  # @Software: PyCharm
  # @Author: Ammoniaw
  # @FileName: make_fraction.py
  # @Date: 2026/8/2
  # @Description: 计算约分
  
  from fractions import Fraction
  
  result = Fraction(1708227363155544, 4636617128565048)
  print(result)  # 7/19
  ```

### 1. 写一个程序，判断给定年份是否为闰年。

> 闰年（Leap Year）是为了弥补因人为历法规定造成的年度天数与地球实际公转周期的时间差而设立的。
> 闰年分为普通闰年和世纪闰年。
> 公历年份是 4 的倍数的，且不是 100 的倍数，为普通闰年（如 2020 年就是闰年）；
> 公历年份是整百数的，必须是 400 的倍数才是世纪闰年（如 2020 年不是世纪闰年，2000 年是世纪闰年）

- leap_year.py

  ```python
  # -*- coding: utf-8 -*-
  # @Software: PyCharm
  # @Author: Ammoniaw
  # @FileName: leap_year.py
  # @Date: 2026/8/2
  # @Description: 判断年份是否为闰年
  # 规则：能被4整除且不能被100整除；被400整除
  
  # 请求用户输入一个年份
  year = int(input("请输入一个年份："))
  if year % 4 == 0 and year % 100 != 0 or year % 400 == 0:
      print(f"{year}是闰年！")
  else:
      print(f"{year}不是闰年！")
  ```

- 小甲鱼还写了一篇关于 [闰年的详细讲解](https://fishc.com.cn/thread-229852-1-1.html)