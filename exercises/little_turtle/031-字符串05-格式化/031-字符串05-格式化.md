# 031-字符串05-格式化

## 问答题

### 0. 请问下面代码会打印什么内容？

- 代码

  ```python
  "{}, {}, {}".format("苹果", "香蕉", "梨")
  ```

- 执行结果：`苹果, 香蕉, 梨`

### 1. 请问下面代码会打印什么内容？

- 代码

  ```python
  "{1}看到{0}就很激动！".format("小甲鱼", "漂亮的小姐姐") 
  ```

- 执行结果 - 使用索引来指定参数的格式化位置

  ```python
  "漂亮的小姐姐看见小甲鱼就很激动"  # 是不是有点自恋了哈哈
  ```

### 2. 请问下面代码会打印什么内容？

- 代码

  ```python
  >>> "我叫{name}，我爱{0}。喜爱{0}的人，运气都不会太差^o^".format(name="小甲鱼", "python")
  ```

- 执行结果

  ```python
  会报错吧，关键字参数应该放在最后进行传递。
  ```

### 3. 请问下面代码会打印什么内容？

- 代码

  ```python
  >>> "{{0}}".format(1)
  ```

- 执行结果：`{0}`在花括号外边套一层花括号，外层是对内层起到“注释“的作用。

### 4. 请问下面代码会打印什么内容？

- 代码

  ```python
  >>> "{{{0}}}".format(1)
  ```

- 执行结果：`{1}`

  > 小甲鱼：首先最外层注释了倒数第二层，那么最终结果将保留一个花括号
  > 接着，内部剩下的 {0}，则正常解析，获取 format() 的第一个参数

### 5. 请问下面代码会打印什么内容？

- 代码

  ```python
  >>> "{{{{{{0}}}}}}".format(1)
  ```

- 执行结果：~~{{{{{1}}}}}~~

- `{{{0}}}' 

  > 数一下，这里存在 6 层花括号包裹，所以不难想象，每 2 层进行一次注释，得到的就是 3 对失去了特殊功能的花括号，所以剩下一个 0 也自然是正经的字符串。

---

## 动动手

### 0. 请编写一个程序，统计字符串中的单词个数（“单词”以空格进行分隔）

- 举例

  ```python
  输入：（空字符串）
  输出：0
  
  输入：Python  
  输出：1
  
  输入：I love FishC
  输出：3
  ```

- 代码实现

  > 上一节学习过split()方法来着，可以直接使用吧；终于有一个我能解决的问题了，sh*t

  ```python
  # -*- coding: utf-8 -*-
  # @Software: PyCharm
  # @Author: Ammoniaw
  # @FileName: count_words.py
  # @Date: 2026/8/16
  # @Description: 统计字符串中的单词个数
  prompt = "请输入字符串:"
  input_str = input(prompt)
  # split返回列表 直接使用len()进行统计即可
  count = len(input_str.split(' '))
  print(f"输出: {count}")
  ```

- 小甲鱼

  ```python
  s = input("请输入测试字符串：")
  print(len(s.split()))
  ```

### 1.请编写一个程序，将用户输入的字符串重新格式化，使得字母和数字相互分隔（即一个字母一个数字相互间隔）

- 举例

  ```python
  输入：FishC1314
  输出：F1i3s1h4C
  
  输入：FishC520
  输出：字符串中数字和字母的数量不满足重新格式化的条件
  
  输入：Python6543210
  输出：6P5y4t3h2o1n0
  ```

- 代码实现

  > 首先得把字母和数字进行分离，然后判断数量是否相等，相等做下一步处理；
  >
  > 但是为什么第三个示例的顺序会是数字在前，字母在后呢?很奇怪，是因为少或多一个数字吗

- 小甲鱼

  > 小甲鱼出的题太神秘了，我现阶段还无法得到一丝领悟！！！

  ```python
  s = input("请输入测试字符串：")
      
  str09 = []
  strAZ = []
      
  # 将数字和字母进行归类存放
  for each in s:
      if each.isdecimal():
          str09.append(each)
      else:
          strAZ.append(each)
      
  len09 = len(str09)
  lenAZ = len(strAZ)
      
  # 如果两个容器的元素个数相差 1 个以上，则不满足重新格式化的条件
  if abs(len09 - lenAZ) > 1:
      print("字符串中数字和字母的数量不满足重新格式化的条件。")
  else:
      if len09 > lenAZ:
          shorter = strAZ
          longer = str09
      else:
          shorter = str09
          longer = strAZ
      
      result = []
      for i in range(len(shorter)):
          result.append(longer[i])
          result.append(shorter[i])
      
      # 由于 longer 是有可能等于 shorter 的，就不必执行下面这一步
      if len(longer) > len(shorter):
          result.append(longer[-1])
      
      print("".join(result))
  ```

  