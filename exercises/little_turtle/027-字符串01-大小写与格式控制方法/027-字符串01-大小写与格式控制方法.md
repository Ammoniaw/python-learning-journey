# 027-字符串01-大小写与格式控制方法

## 问答题

### 0. `casefold()` 和 `lower()` 两个方法都是将所有的字母变成小写，那它们有什么区别呢？

- `casefold()`似乎可以将除非英文的字母转换为小写。

### 1.请问下面代码执行的结果是？

- 代码

  ```python
  >>> "-520".zfill(10)
  ```

- 结果

  ```python
  # zfill方法 - zerofill默认采用0填充空白字符 '-000000520'
  ```

### 2. 请问 "-520".zfill(10) 和 "-520".rjust(10, "0") 的执行结果一样吗？

- 不一样 - zfill识别负号

  ```python
  >>> "-520".zfill(10)
  '-000000520'
  >>> "-520".rjust(10, "0")
  '000000-520'
  ```

### 3. 请问下面代码执行的结果是？

- 代码

  ```python
  >>> "-520".center(6, "0").zfill(10)
  ```

- 执行顺序：从左向右依次执行

  ```python
  "-520".center(6, "0")  # 6个字符宽度 空白字符使用数字0填充 0-5200
  # 之后执行.zfill(10) 修改为10个字符宽度 '00000-5200'
  
  ```

### 4. 请问下面代码执行的结果是？

- 代码

  ```python
  >>> "I love FishC".swapcase()[::-1]
  ```

- 结果： `swapcase()` - 返回一个大小写字符翻转的新字符串

  ```python
  # 大小写翻转
  "i LOVE fISHc"
  # 最后是序列的切片取反操作 - 顺序颠倒
  'cHSIf EVOL i'
  ```

### 5. 请使用一行代码来检测列表中的每个元素是否为回文数，并返回一个结果为回文数构成的列表

- 代码

  ```python
  lists = [x for x in list1 if x == x[::-1] ]
  ```

## 动动手

### 0. 请按照以下规则整理一个给定的字符串 s

- 要求

  ```python
  """
  一个整理好的字符串中，两个相邻字符 s[j] 和 s[j+1]，其中 0 <= j <= s.length - 2，要满足如下条件：
  若 s[j] 是小写字符，则 s[j+1] 不可以是相同的大写字符
  若 s[j] 是大写字符，则 s[j+1] 不可以是相同的小写字符
  如果 s[j] 和 s[j+1] 满足以上两个条件，则将它们一并删除
  """
  ```

- 字符串示例

  ```python
  整理前："AABaAbCc"
  整理后："AABaAbCc" -> "AABbCc" -> "AACc" -> "AA"
  ```

- 代码实现 - 小甲鱼

  > 依旧没有一点想法

  ```python
  s = input("请输入一个字符串：")
      
  res = []
  for each in s:
      if res and res[-1].lower() == each.lower() and res[-1] != each:
          res.pop()
      else:
          res.append(each)
      
  for each in res:
      print(each, end='')
  ```

  

### 1. 给定的字符串 s 是按照如下规则存放的：它的偶数下标为小写英文字母，奇数下标为正整数。

- 要求 - 编写代码，将奇数下标的数字转换为相对于上一个字母偏移后的字母。

  ```python
  比如 s = "a1b2c3" 转换后的结果是 "abbdcf"（a1 -> ab，b2 -> bd，c3 -> cf）；s = "x7y8z9" 转换后的结果是 "xeygzi"（遇到最后字母 z ，则从 a 继续计算偏移）
  ```

- 代码实现

  > 有点感觉：好像得使用到`chr()`或者`ord()`

  ```python
  # -*- coding: utf-8 -*-
  # @Software: PyCharm
  # @Author: Ammoniaw
  # @FileName: 01_offset.py
  # @Date: 2026/8/9
  # @Description: 偏移字母
  
  s = "x7y8z9"
  new_s = ''
  for i in range(len(s)):
      if i % 2 == 0:
          new_s += s[i]
      else:
          if ord(s[i - 1]) + int(s[i]) > ord('z'):
              new_s += chr(ord(s[i - 1]) + int(s[i]) - ord('z') + ord('a') - 1)
          else:
              new_s += chr(ord(s[i - 1]) + int(s[i]))
  
  print(new_s)
  ```

- 小甲鱼

  ```python
  s = input("请按规则输入一个字符串：")
      
  length = len(s)
  # 获取字母 a 的编码值
  base = ord('a')
      
  # 从第一个元素开始，每次迭代跳过一个元素
  for i in range(0, length, 2):
      # ord(s[i]) - base 操作得到一个字母的偏移值，比如 b 就是 1
      # 跟 26 求余数的作用是防止溢出，循环计算偏移
      shift = chr((ord(s[i]) - base + int(s[i+1])) % 26 + base)
      print(s[i]+shift, end="")
  ```

  

