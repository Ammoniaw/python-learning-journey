# 005-是时候讲讲代码了

## 问答题

### 0. 在 Python 中，一个等于号（=）和两个等于号（==）的功能是一样的吗？

- 不同, `=`是赋值运算符，常用于变量赋值；`==`是比较运算符，常用与判断两边操作数的值是否相等

### 1.  请问下面代码为什么会报错？

- 异常代码

  ```
  >>> 3 < = 4
  SyntaxError: invalid syntax
  ```

- 不应该分开书写 ：`3 <= 4`

### 2. 请问下面代码返回的 True 还是 False？

- 代码

  ```
  >>> 3 <= 5 >= 4
  ```

- ~~`False` ; 3<=5判断为True;True的值为1,随后判断1>=4,结果是False.~~ Python 支持将多个比较操作符串联起来，这个表达式将对比 3 <= 5 和 5 >= 4，两者都成立，所以结果自然是 True

### 3. 请问下面代码返回的值是什么？

- 代码

  ```
  >>> 1 + 1 >= 2
  ```

- True,Python首先计算 表达式 1+1的值，随后与2进行比较。

### 4. 请问下面代码存在什么问题？

- 代码

  ```python
  if guess == 8:
      print("你是小甲鱼心里的蛔虫嘛？！")
     print("哼，猜中了也没奖励！")
  else:
      print("猜错啦，小甲鱼现在心里想的是8！")
  ```

- 缩进问题，python代码的缩进非常重要。

### 5. 请问下面 A、B、C、D 四个表达式中，哪些将返回 True？

- 表达式

  ```python
  A. 'FishC' == '''FishC'''
  B. "小甲鱼" == "小乌龟"
  C. 520 == int(520.1314)
  D. 9 == int(9.99)
  ```

- A、C、D

---

## 动动手

### 1.  请按下面要求实现程序。

- 要求A：打开 IDLE 的编辑模式版

- 要求B：将下面代码输入

- 要求C：将代码保存到桌面，并命名为 test

- 要求D：执行程序Po

- 代码

  ```python
  num1 = input("请输入第一个整数：")
  num2 = input("请输入第二个整数：")
  
  if num1 < num2:
      print("第一个数比第二个数小！")
  
  if num1 > num2:
      print("第一个数比第二个数大！")
  
  if num1 == num2:
      print("第一个数和第二个数一样大！")
  ```

- compare.py

  ```python
  # -*- coding: utf-8 -*-
  # @Software: PyCharm
  # @Author: Ammoniaw
  # @FileName: compare.py
  # @Date: 2026/8/1
  # @Description: 比较数字
  num1 = int(input("请输入第一个整数："))
  num2 = int(input("请输入第二个整数："))
  
  if num1 < num2:
      print("第一个数比第二个数小！")
  
  if num1 > num2:
      print("第一个数比第二个数大！")
  
  if num1 == num2:
      print("第一个数和第二个数一样大！")
  ```

  

