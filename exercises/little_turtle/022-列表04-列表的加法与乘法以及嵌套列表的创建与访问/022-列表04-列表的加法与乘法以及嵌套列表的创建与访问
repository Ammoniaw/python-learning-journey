

# 022-列表04-列表的加法与乘法以及嵌套列表的创建与访问

## 问答题

### 0. 请问 == 运算符和 is 运算符有什么区别呢？

- `==`运算符用于判断两边是否相等，而`is`运算符用于判定两边是否引用同一个对象

### 1. 请问下面代码的执行结果是？

- 代码

  ```python
  >>> [[1, 2, 3], [4, 5, 6]] + [7, 8, 9]
  ```

- 结果 - 加法是直接拼接到列表的末尾 - 类似于 append

  ```python
  [[1, 2, 3], [4, 5, 6], [7, 8, 9]
  ```

### 2. 请问下面代码的执行结果是？

- 代码

  ```python
  >>> len([[1, 2, 3], [4, 5, 6], [7, 8, 9]])
  ```

- 3，`len()`的参数是一个列表，列表里面有3个元素，不过每一个元素都是一个列表。

### 3. 请问下面代码的结果是返回 True 还是 False 呢？

- 代码

  ```python
  >>> a = 250
  >>> b = 250
  >>> a is b
  ```

- True,相同的数据(对象)使用了不同的变量进行引用，所以使用`is`判断都是同一个对象。

- 小甲鱼：

  > 出于性能优化方面的考虑，Python 在内部为 -5~256 范围内的整数维护了一个数组，起到缓存的作用。
  > 这样，每次你试图创建一个 -5~256 范围内的整数时，Python 都会从这个数组中返回相对应的引用，而不是重新开辟一块新的内存空间存放

### 4. 请问下面代码的结果是返回 True 还是 False 呢？

- 代码

  ```python
  >>> a = 1000
  >>> b = 1000
  >>> a is b
  ```

- ~~True,理由同上。~~
- 因为有维护数组的原因，超过了256,是重新创建新的对象，所以返回False.

### 5. 既然有二维列表，那么三维列表应该也是“同理可得”的东西，请大家尝试创建一个简单的三维列表吧？

- 代码实现

  ```python
  # 最简单的直接赋值
  cube_list = [
      [
          [1,2,3]
      ],
      [
          [4.5.6]
      ]
  ]
  
  # 使用for循环和列表乘法
  list_z = [0, 0, 0]
  for j in range(3):
      list_z[j] = [0] * 3
      for k in range(3):
          list_z[j][k] = [0] * 2
  ```

  

---



## 动动手

###  0. 请根据下面的内存关系图，分别创建出 x、y 和 z 三个不同的列表。

- 图片

  ![img](./assets/015135ffjip61oa12non4n.png)

- 代码实现

  ```python
  # -*- coding: utf-8 -*-
  # @Software: PyCharm
  # @Author: Ammoniaw
  # @FileName: nested_list.py
  # @Date: 2026/8/5
  # @Description: 创建嵌套列表
  
  # 列表x 直接赋值
  list_x = [0, 0, 0]
   
  # 列表y - 二维列表
  list_y = [0, 0, 0]
  for j in range(3):
      list_y[j] = [0] * 3
  
  # 列表z - 三维列表
  list_z = [0, 0, 0]
  for j in range(3):
      list_z[j] = [0] * 3
      for k in range(3):
          list_z[j][k] = [0] * 2
  
  print(f"{list_x=}")
  print(f"{list_y=}")
  print(f"{list_z=}")
  
  ```

- 小甲鱼

  > 二维列表和三维列表倒是正确，但是x列表对同一个列表的引用掌握还不是很好，题目阅读理解错误。

  ```python
  # x列表 每个子元素都指向同一个列表 -  [0,0,0]
  x = [[0] * 3] * 3
  
  # y列表
  y = [0] * 3
  for i in range(3):
      y[i] = [0] * 3
      
  # z列表
  list_z = [0, 0, 0]
  for j in range(3):
      list_z[j] = [0] * 3
      for k in range(3):
          list_z[j][k] = [0] * 2
  
  ```

  

### 1. 上一节的课后作业我们提到了“摩尔投票法”，这种方法尤其适用于在任意多的选项中找到数量占比最多的那一个

- 那么这一次我们修改一下要求，编写代码，利用“摩尔投票法”来找出占比数量最多的两个元素（注意：这两个元素的数量都需要超过总数的三分之一）

- PS: 还是看不懂诶，虽然知道了摩尔投票法的原理，但是这种变体，还是掌握的不是特别清晰，救命啊

- 代码实现 - 小甲鱼

  ```python
  nums = [1, 1, 2, 1, 3, 2, 3, 2]
      
  major1 = major2 = nums[0]
  count1 = count2 = 0
      
  # 对抗阶段
  for each in nums:
      # 第一次for循环运行时 假设第一个元素是两个元素之一 之后continue
      if major1 == each:
          count1 += 1
          continue
          
      if major2 == each:
          count2 += 1
          continue
      
      if count1 == 0:
          major1 = each
          count1 = 1
          continue
      
      if count2 == 0:
          major2 = each
          count2 = 1
          continue
      
      count1 -= 1
      count2 -= 1
      
  # 统计阶段
  if nums.count(major1) > len(nums) / 3:
      print(major1)
  if nums.count(major2) > len(nums) / 3:
      print(major2)
  ```

  