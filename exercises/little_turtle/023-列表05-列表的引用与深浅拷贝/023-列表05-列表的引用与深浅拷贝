# 023-列表05-列表的引用与深浅拷贝

## 问答题

###  0. 请问你是如何理解 ”在 Python 中，变量不是盒子” 这句话的？

- 变量不是存储数据的“盒子”，而更像是盒子的标签，而这个标签可以随时指向任意一个“盒子”，变量只是对对象或数据的引用。

### 1. 请问下面代码执行后，y 的值是多少？

- 代码

  ```python
  >>> x = 1
  >>> y = x
  >>> x = 2
  ```

- y=1

### 2. 请问下面代码执行后，x == y 返回的结果是 True 还是 False?

- 代码

  ```python
  >>> x = [1, 2, 3]
  >>> y = x
  >>> y[1] = 1
  >>> x == y
  ```

-  ~~相同~~True[^1]，变量x和y指向的是同一个列表

### 3. 请问下面代码实现的是浅拷贝还是深拷贝？

- 代码

  ```python
  >>> x = [[1, 2, 3], [4, 5, 6]]
  >>> y = x[:]
  ```

- 浅拷贝：是列表的切片

### 4.  请问下面代码执行后，列表 x 和 y 的内容分别是什么？

- 代码

  ```python
  >>> x = [[1, 2, 3], [4, 5, 6]]
  >>> y = x.copy()
  >>> y.append(7)
  >>> y[1].append(8)
  ```

- 执行结果

  ```python
  # 列表y
  [[1, 2, 3], [4, 5, 6, 8], 7]
  # 列表x则不会有影响 浅拷贝只能拷贝第一层数据，其余仅拷贝其引用
  y.append(7) # 并不会影响到第二层的引用 所以列表x不会发生任何改变
  ```

### 5. 请问下面代码执行后，列表 s 的内容是什么？

- 代码

  ```python
  >>> s = [1]
  >>> s.append(s)
  ```

- 执行结果 - 又错了md:x:

  ```python
  # 错误的
  [1,[1]]
  ```

- 小甲鱼

  ```python
  # 正确输出
  [1, [...]]
  # 因为列表s是指向自身的引用，所以就相当于无限循环。
  ```

  

---

## 动动手

### 0.  创建一个 88 x 88 的随机整数矩阵（二维列表），然后匹配用户输入的整数是否与其中某元素相等，如果相等则打印其行号和列号.

- 要求
  - 要求1：随机整数取值范围 0~1024
  - 要求2：需找出所有匹配的元素来

- 程序实现效果![img](./assets/160855enpyuvruvg4tyimo.png)

- 代码实现

  > 这块儿看了官方的[嵌套的列表推导式](https://docs.python.org/zh-cn/3/tutorial/datastructures.html#nested-list-comprehensions) 做出来的。

  ```python
  # -*- coding: utf-8 -*-
  # @Software: PyCharm
  # @Author: Ammoniaw
  # @FileName: matrix.py
  # @Date: 2026/8/6
  # @Description: 生成随机数矩阵列表 找出指定数
  import random
  
  target_number = int(input("请输入一个待匹配的整数："))
  
  matrix_list = []
  for i in range(1, 89):
      list1 = []
      for j in range(1, 89):
          # 要求1 随机整数取值范围 0-1024
          number = random.randint(0, 1024)
          if target_number == number:
              # 要求2 找出所有匹配元素 并输出行、列号
              print(i, j)
          list1.append(number)
      matrix_list.append(list1)
  # print(matrix_list)
  
  ```

### 1.请编程找出矩阵中的幸运数字

- 说明

  > 假设给定一个 m * n 的矩阵（矩阵中数值的取值范围是 0~1024，且各不相同），如果某一个元素的值在同一行的所有元素中最小，并且在同一列的所有元素中最大，那么该元素便是幸运数字

- 假设给定的矩阵如下

  ```python
  matrix = [[10, 36, 52],
            [33, 24, 88],
            [66, 76, 99]]
  ```

​	那么输出结果应该是 66（同时满足同一行的所有元素中最小，并且在同一列的所有元素中最大）

- 代码实现 -- 又错了 shit:x:

  ```python
  # -*- coding: utf-8 -*-
  # @Software: PyCharm
  # @Author: Ammoniaw
  # @FileName: lucky_number.py
  # @Date: 2026/8/6
  # @Description: 找出矩阵中的幸运数字
  
  matrix = [[10, 36, 52],
            [33, 24, 88],
            [66, 76, 99]]
  min_list = []
  for i in range(3):
      # 找出每个子列表中最小的数字 添加到列表中
      min_number = min(matrix[i])
      min_list.append(min_number)
  # 找出最大的那个即是幸运数字
  lucky_number = max(min_list)
  
  print(lucky_number)
  
  ```

  holly shit ,做法也不对，应该同时满足，同时满足同一行的所有元素中最小，并且在同一列的所有元素中最大，这次是瞎猫碰上死耗子了。

- 小甲鱼

  ```python
  matrix = [[10, 36, 52],
            [33, 24, 88],
            [66, 76, 99]]
      
  row = len(matrix)  # 计算出有多少个列表，即行的数量
  col = len(matrix[0])  # 计算第一个子列表的数量 即列的数量
      
  min_row = [1024] * row  # 创建一个全是[1024,1024...]的列表
  max_col = [0] * col  # 创建一个全是[0, 0...]的列表
      
  # 遍历矩阵中的每一个元素 分别与两个列表进行比较 将他们分别进行存储
  for i in range(row):
      for j in range(col):
          # 找到每行中最小的元素，并将它们存放到列表min_row中 
          min_row[i] = matrix[i][j] if matrix[i][j] < min_row[i] else min_row[i]    # min(matrix[i][j], min_row[i])
          # 找到每列中最大的元素，并将它们存放到列表max_col中
          max_col[j] = matrix[i][j] if matrix[i][j] > max_col[j] else max_col[j]    # max(matrix[i][j], max_col[j])
      
  # 遍历矩阵中的每一个元素
  # 判断是否同时满足“同一行的所有元素中最小”和“同一列的所有元素中最大”
  for i in range(row):
      for j in range(col):
          if matrix[i][j] == min_row[i] and matrix[i][j] == max_col[j]:
              print(matrix[i][j])
  ```

  和DS沟通了一下，解题思路大概是：首先确定目标矩阵的行与列的数量，然后根据数量创建一个全是1024列表和一个全是0的列表，然后循环遍历矩阵，分别筛选更新出最大值和最小值，将其分别存储到列表中，最后遍历矩阵，判断两个列表中的元素是否同时满足那两个条件。

[^1]: 注意审题啊(#`O′)
