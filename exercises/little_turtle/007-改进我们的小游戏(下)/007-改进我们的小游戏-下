# 007-改进我们的小游戏-下

## 问答题

### 0. “所有使用算法生成的随机数，都是伪随机数！”，这种说法对吗？

- 对，算法毕竟可以复现，由同一种算法产生的随机数，可以经由同一种条件来生成一样的"随机数"

### 1. 如果要使用 random 模块，第一步应该怎么做？

- 导入模块：`import random`

### 2. 调用 `random.randint(1, 10)` 函数生成了一个随机数，是否有可能会出现 10 这个数？

- 会，`randint()`生成的整数范围为[num1,num2],即num1 <= 随机数 <= num2

### 3. 请自学 random 文档（[传送门](https://fishc.com.cn/thread-145254-1-1.html)），然后告诉小甲鱼下面这句代码的作用是什么？

- 代码

  ```python
  random.choice("ilovefishc")
  ```

- 从指定序列返回一个随机元素 - 此处就是从字符串中随机返回一个字符

### 4. 请问下面两个 print() 语句打印的结果是否相同，为什么？

- 代码

  ```python
  import random
  
  random.seed(1)
  print(random.randint(1, 10), random.randint(1, 100), random.randint(1, 1000))
  
  random.seed(1)
  print(random.randint(1, 10), random.randint(1, 100), random.randint(1, 1000))
  ```

- 当提供的种子一样时，random方法将生成一样的伪随机数

---

## 动动手

### 0. 请自学 random 文档（[传送门](https://fishc.com.cn/thread-145254-1-1.html)），并选择一个合适的函数，在 0~99 之间随机抽取一个偶数。

- random-range.py

  ```python
  # -*- coding: utf-8 -*-
  # @Software: PyCharm
  # @Author: Ammoniaw
  # @FileName: random_range.py
  # @Date: 2026/8/2
  # @Description: random模块 在0-99之间随机抽取一个偶数
  import random
  # 为该函数指定开始、结束以及步长
  num = random.randrange(0, 100, 2)
  print(num)
  ```

### 1. 请自学 random 文档（[传送门](https://fishc.com.cn/thread-145254-1-1.html)），编写一个双色球的开奖模拟程序。

- 科普规则

  > “双色球”每注投注号码由 6 个红色球号码和 1 个蓝色球号码组成。红色球号码从 1~33 中选择（由于每次抽取后不放回，所以不会出现重复数字）；蓝色球号码从 1~16 中选择

- 提示

  - `random.sample()`方法

  - 代码

    ```python
    >>> print(x)
    [1, 2, 3, 4, 5]
    >>> print(*x)
    1 2 3 4 5
    ```

- double_color_ball.py

  ```python
  # -*- coding: utf-8 -*-
  # @Software: PyCharm
  # @Author: Ammoniaw
  # @FileName: double_color_ball.py
  # @Date: 2026/8/2
  # @Description: 模拟双色球
  import random
  
  red_balls = list(range(1, 34))
  blue_balls = list(range(1, 16))
  # sample() 从指定序列population中抽取k个不重复元素并返回 - 此处可以直接使用range()函数生成，我就不修改了
  red_numbers = random.sample(population=red_balls, k=6)
  blue_number = random.randint(1, 16)
  print("开奖结果是:", *red_numbers)  # 结果解包
  print("特别号码是:", blue_number)
  ```

  