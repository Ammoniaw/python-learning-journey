# [名称，如：链式比较 / 摩尔投票法]

## 🧠 我的“错误脑回路”（当时是怎么想的？）

> 写下你最原始、甚至有点愚蠢的想法，这是以后复习最“打脸”也最有效的地方。
> *（例如：我以为 `a < b < c` 是先算左边 `a < b` 得到 True，再拿 True 去和 c 比较……）*

## ✅ 正确的“底层逻辑”（一句话击穿本质）

> 用最通俗的大白话说清楚，不要抄定义。
> *（例如：Python 的链式比较本质是 `and` 连接，且中间的变量 `b` 只计算一次。）*

## 💻 救命代码（跑得通的示例 + 特例）

```python
# 好记性不如烂笔头，放上能立刻唤醒记忆的最小示例
print(3 < 4 < 5)   # True -> 相当于 3 < 4 and 4 < 5
print(3 < 4 > 2)   # True -> 相当于 3 < 4 and 4 > 2（注意不是连等！）
```

## 🔗 记忆锚点（联想法/生活比喻）

> 给枯燥的算法一个“外号”或画面感。
> *（例如：摩尔投票法就像“不同阵营士兵同归于尽”，最后活着的肯定是人数最多的阵营。）*

## 📂 关联题目（放在 exercises 里的哪一题用到了？）

*（例如：文件或者示例代码）*